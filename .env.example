# Local Browser Automation Bridge does not require any environment variables
# for the local demo. The backend reads its config from apps/backend/src/config.ts
# (port 4000 by default).
#
# If you want to override the backend port, copy this file to `.env` and set:
#
#   PORT=4000
